fh# Example .env for Task_Manager_BE
# Copy this to `.env` and fill values for your environment.

# App
PORT=4000
SECRET=change_this_secret
PASSWORD_EXPIRE_DAYS=60
REQUIRE_REDIS=false

# Redis (set if REQUIRE_REDIS=true)
# Example for local: redis://127.0.0.1:6379
REDIS_URL=redis://127.0.0.1:6379

# SMTP (for OTP and notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=nikhithakondreddy590@gmail.com
SMTP_PASS=tzki fbwi fmzr nnuv  # Your App Password
SMTP_FROM=nikhithakondreddy590@gmail.com


# Gmail (legacy used in some controllers)
GMAIL_USER = nikhithakondreddy590@gmail.com
GMAIL_PASS = "tzki fbwi fmzr nnuv"

# AWS S3 (for uploads)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET_NAME=

# Other
# Set to your tenant id for testing (or override per request header)
DEFAULT_TENANT=tenant_1
