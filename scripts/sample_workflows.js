// Sample flow runner (lighter than test): demonstrates the happy path
const { run } = require('../test/workflow_flow.test');

// Run once to demo the flow (requires server running and env vars optionally set)
run();
