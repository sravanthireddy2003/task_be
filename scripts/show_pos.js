const fs=require('fs');const path=require('path');const s=fs.readFileSync(path.join(__dirname,'..','src','controllers','Tasks.js'),'utf8');const pos=33075;const start=Math.max(0,pos-200), end=Math.min(s.length,pos+200);const snippet=s.slice(start,end);const linesBefore=s.slice(0,start).split(/\r?\n/).length;console.log('Approx line start:', linesBefore);console.log('Context:\n',snippet);